#!/bin/sh

PLZ_NPM_LOCATION="${1}";

if [ "${PLZ_NPM_LOCATION}x" = "x" ]; then
  echo;
  echo "Usage: plz npm-install <location>";
  echo;

  exit 1;
fi;

cd "${PLZ_NPM_LOCATION}";
if [ -e package.json ]; then
  ./_node/npm install;
fi;
